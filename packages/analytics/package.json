{
	"name": "analytics",
	"version": "0.2.0",
	"private": true,
	"description": "Shared analytics event taxonomy for Downbeat Academy — event names and property types, no runtime",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^4.1.6"
	}
}

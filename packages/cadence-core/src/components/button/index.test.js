import { Button } from './index';
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';

// describe('<Button />', () => {
//     it('renders', () => {
//         render(<Button text='Button test text' />);
//         expect(screen.getByRole('button', { text: 'Button test text' }).toBeInTheDocument())
//     })
// })

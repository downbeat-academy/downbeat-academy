/** Which side of the anchor an overlay is placed on. */
type OverlaySide = 'top' | 'right' | 'bottom' | 'left'

/** Alignment along that side. */
type OverlayAlign = 'start' | 'center' | 'end'

export type { OverlaySide, OverlayAlign }

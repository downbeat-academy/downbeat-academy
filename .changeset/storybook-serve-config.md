---
"cadence-core-web-components": patch
---

Add serve configuration for production Storybook hosting. Adds `start` script and `serve` dependency matching cadence-core so the web components Storybook can run on Railway and other platforms.

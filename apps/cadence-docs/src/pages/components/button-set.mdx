import { Button, ButtonSet } from '@downbeat-academy/cadence-core';

# Button set

The `Button Set` component is used as as container for grouping like buttons and actions.

Example

<ButtonSet>
	<Button text="Primary button" variant="primary" size="default" />
	<Button text="Secondary button" variant="secondary" size="default" />
</ButtonSet>

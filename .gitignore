DS_STORE
DS_Store
DS_store
.DS_store
.DS_Store
.DS_STORE

.mcp.json

# Personal Claude Code overrides — shared config lives in .claude/settings.json
.claude/settings.local.json

# Environment files — secrets live in Infisical, never in git
.env
.env.local
.env.*.local
.env.test
apps/**/.env
apps/**/.env.local
apps/**/.env.*.local

node_modules

.turbo
packages/**/.turbo
apps/**/.turbo

# Cypress artifacts
cypress/screenshots
cypress/videos
apps/**/cypress/screenshots
apps/**/cypress/videos
*.mp4